import { IGameProps } from './Game-types';
import './Game.scss';

export function Game(_props: IGameProps): JSX.Element {
    return (
        <div className="game">
            Game
        </div>
    );
}
